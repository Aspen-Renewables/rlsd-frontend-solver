const Page = () => {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center">
      <p>Admin needs to set the password environment variable in vercel</p>
    </div>
  );
};

export default Page;
